1 -->
ls -al ~/.ssh
ssh-keygen -t ed25519 -C "navneettiwarinew270103@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
ssh -> ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEVfkosCmlViSne9Tn2mCie0TkY/eDg7xs7k6R60k0kN navneettiwarinew270103@gmail.com
ssh -T nvnetpd10.com

2 -->
git init 
git add .
git commit -m "done"
git remote add origin git@github.com:your-nvnetpd10/my-first-repo.git
git push -u origin main

3 --> git clone https://github.com/your-nvnetpd10/my-first-repo.git

4 --> git add test.txt 
git commit -m "file added"
git push -u origin main

5 --> git branch ? -> main 
git pull 
git checkout -b f1
git checkout main
git checkout -b f2

6 -> git checkout main 
git merge f1
git add .
git commit -m "conflict resolved" 
git push 
git log --oneline --graph --all

7 --> git init git-divergence-demo
cd git-divergence-demo
echo "Version 1" > file.txt
git add .
git commit -m "Initial commit"
git checkout -b feature
echo "Feature branch update" > file.txt
git commit -am "Update in feature branch"
git checkout main
echo "Main branch update" > file.txt
git commit -am "Update in main branch"
git log --oneline --graph --all

8 --> Initializes a new, empty Git repository in the current directory.
Creates a copy (clone) of an existing remote repository.

9 --> git branch = makes or lists branches.
git checkout = moves you between branches.

10 --> git fetch → just downloads updates.
git pull → downloads and merges them.

11 ->To combine multiple commits into a single,

12  --> reset = erase history.
revert = add a new "undo" commit safely.

13 --> git branch -d branch_name -> safe delete (only if merged)
git branch -D branch_name -> force delete (even if not merged)
